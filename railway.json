# This file is used to configure your Railway project.but it is not required. as i already used all the free service  and i cant deploy there anymore
# You can remove this file if you don't need it.
{
    "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "./Dockerfile",
        "watchPatterns": [
            "requirements.txt",
            "src/**",
            "railway.json",
            "Dockerfile"
        ]
    }
}
